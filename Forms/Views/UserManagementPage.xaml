<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:Forms.Converters"
             x:Class="Forms.Views.UserManagementPage"
             Title="Gestión de Usuarios">
    <ContentPage.Resources>
        <converters:RoleToColorConverter x:Key="RoleToColor"/>
    </ContentPage.Resources>
    <Grid RowDefinitions="Auto,*,Auto" BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}">
        <Button Grid.Row="0" Text="Nuevo Usuario" Clicked="OnCreateUserClicked" 
                BackgroundColor="{AppThemeBinding Light=SkyBlue, Dark=DeepSkyBlue}" TextColor="White" Margin="10" CornerRadius="10"/>
        
        <CollectionView x:Name="UsersList" Grid.Row="1" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="10" Padding="15" CornerRadius="10"
                           BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}">
                        <Grid ColumnDefinitions="*,Auto,Auto,Auto">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding UserName}" FontSize="18" FontAttributes="Bold"
                                       TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"/>
                                <Label Text="{Binding Email}" FontSize="14" TextColor="{AppThemeBinding Light=Gray, Dark=LightGray}"/>
                                <Label Text="{Binding Role}" FontSize="12" 
                                       TextColor="{Binding Role, Converter={StaticResource RoleToColor}}"/>
                            </VerticalStackLayout>
                            <Button Grid.Column="1" Text="Editar" Clicked="OnEditRoleClicked" 
                                    CommandParameter="{Binding .}" BackgroundColor="{AppThemeBinding Light=Orange, Dark=DarkOrange}" 
                                    TextColor="White" Margin="5"/>
                            <Button Grid.Column="2" Text="Eliminar" Clicked="OnDeleteClicked" 
                                    CommandParameter="{Binding .}" BackgroundColor="{AppThemeBinding Light=Red, Dark=#FF6B6B}" 
                                    TextColor="White" Margin="5"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
